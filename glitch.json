{
    "install": "pip3 install --user opencv-python;rm -rf .cache;pip3 install --user aiortc;rm -rf .cache;pip3 install --user aiohttp;rm -rf .cache;pip3 install -r requirements.txt;rm -rf .cache",
    "start": "PYTHONUNBUFFERED=true python3 server.py",
    "watch": {
      "ignore": [
        "\\.pyc$"
      ],
      "install": {
        "include": [
          "^requirements\\.txt$"
        ]
      },
      "restart": {
        "include": [
          "\\.py$",
          "^start\\.sh"
        ]
      },
      "throttle": 1000
    }
  }
  